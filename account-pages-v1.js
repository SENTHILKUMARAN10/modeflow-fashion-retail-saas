(function(){
  'use strict';
  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
  const regions={IN:'India',US:'United States',GB:'United Kingdom',AE:'United Arab Emirates',SG:'Singapore',AU:'Australia',CA:'Canada'};
  const prices={INR:{monthly:'₹399',annual:'₹3,990'},USD:{monthly:'$4.99',annual:'$49.99'}};
  const currencySymbol=c=>c==='USD'?'$':'₹';
  const state={region:localStorage.getItem('mf_region')||localStorage.getItem('modeflow-region')||'IN',currency:localStorage.getItem('mf_currency')||'INR',interval:localStorage.getItem('mf_plan_interval')||'monthly'};
  const nav=document.querySelector('.sidebar nav');if(!nav)return;
  let workspace=null,billingInfo=null;
  const notice=m=>typeof toast==='function'?toast(m):alert(m);

  function addNav(view,num,label){if(document.querySelector(`.nav[data-view="${view}"]`))return;const b=document.createElement('button');b.className='nav';b.dataset.view=view;b.innerHTML=`<i>${num}</i><span>${label}</span>`;nav.appendChild(b);b.addEventListener('click',()=>go(view));}
  addNav('subscription','08','Subscription');addNav('region','09','Region & currency');

  const main=document.querySelector('.app main');if(!main)return;
  if(!$('#subscription'))main.insertAdjacentHTML('beforeend',`<section id="subscription" class="view"><div class="page-intro"><div><p class="kicker">SALESDESK PRO</p><h3>Simple pricing, built to <em>grow with you.</em></h3><p class="mf-section-copy">Secure recurring billing is handled through Razorpay when your production payment configuration is active.</p></div><span class="mf-settings-status" id="mfSubscriptionStatus">Checking plan…</span></div><div class="mf-account-grid"><article class="mf-account-card featured"><p class="kicker light">PRO PLAN</p><h3>Everything your business needs.</h3><div class="mf-plan-switch"><button data-interval="monthly">Monthly</button><button data-interval="annual">Annual</button></div><div class="mf-plan-price" id="mfPlanPrice">₹399 <span>/ month</span></div><ul class="mf-feature-list"><li>Sales, billing and professional invoices</li><li>Products, services and inventory alerts</li><li>Customer records and receivables</li><li>Expenses and profit visibility</li><li>Business analytics and realtime cloud workspace</li><li>Secure multi-user business data separation</li></ul><div class="mf-billing-note" id="mfBillingNote">Pay securely through automated checkout. Access is activated only after the payment provider verifies the subscription.</div><div class="mf-account-actions"><button class="btn secondary" id="mfPayNow">Subscribe securely</button><button class="btn ghost" id="mfCancelPlan" type="button" hidden>Cancel at period end</button></div></article><article class="mf-account-card"><p class="kicker">BILLING DETAILS</p><h3>Your current setup</h3><div class="mf-region-preview"><span>Region</span><strong id="mfBillingRegion">India</strong></div><div class="mf-region-preview"><span>Currency</span><strong id="mfBillingCurrency">INR · ₹</strong></div><div class="mf-region-preview"><span>Plan status</span><strong id="mfCurrentPlan">Loading…</strong></div><div class="mf-region-preview"><span>Current period</span><strong id="mfCurrentPeriod">—</strong></div><div class="mf-account-actions"><button class="btn primary" id="mfOpenRegion">Change region & currency</button></div></article></div></section>`);

  if(!$('#region'))main.insertAdjacentHTML('beforeend',`<section id="region" class="view"><div class="page-intro"><div><p class="kicker">PREFERENCES</p><h3>Your region. Your <em>currency.</em></h3><p>Choose how SalesDesk should display pricing and business values. You can change this anytime.</p></div></div><div class="mf-account-grid"><article class="mf-account-card"><div class="mf-region-form"><label>Business region<select id="mfRegionSelect">${Object.entries(regions).map(([v,l])=>`<option value="${v}">${l}</option>`).join('')}</select></label><label>Display currency<select id="mfCurrencySelect"><option value="INR">INR — Indian Rupee ₹</option><option value="USD">USD — US Dollar $</option></select></label><div class="mf-region-preview"><div><small>PREVIEW</small><strong id="mfCurrencyPreview">₹399</strong></div><span id="mfRegionPreview">India · INR</span></div><div class="mf-account-actions"><button class="btn primary" id="mfSaveRegion">Save preferences</button></div></div></article><article class="mf-account-card"><p class="kicker">HOW IT WORKS</p><h3>Consistent across every screen.</h3><p class="mf-section-copy">Your selected region and currency are saved for SalesDesk pricing and business values.</p><div class="mf-region-preview"><span>Monthly plan</span><strong id="mfMonthlyMini">₹399</strong></div><div class="mf-region-preview"><span>Annual plan</span><strong id="mfAnnualMini">₹3,990</strong></div></article></div></section>`);

  function go(id){$$('.view').forEach(v=>v.classList.toggle('active-view',v.id===id));$$('.nav').forEach(n=>n.classList.toggle('active',n.dataset.view===id));const titles={subscription:['ACCOUNT','Subscription & pricing'],region:['PREFERENCES','Region & currency']};if(titles[id]){if($('#eyebrow'))$('#eyebrow').textContent=titles[id][0];if($('#title'))$('#title').textContent=titles[id][1];}if(id==='subscription')loadBilling();if(window.SalesDeskFooters?.refresh)window.SalesDeskFooters.refresh();if(window.innerWidth<820)window.scrollTo({top:0,behavior:'smooth'});}
  function render(){const p=prices[state.currency]||prices.INR,annual=state.interval==='annual';$$('.mf-plan-switch button').forEach(b=>b.classList.toggle('active',b.dataset.interval===state.interval));if($('#mfPlanPrice'))$('#mfPlanPrice').innerHTML=`${annual?p.annual:p.monthly} <span>/ ${annual?'year':'month'}</span>`;if($('#mfBillingRegion'))$('#mfBillingRegion').textContent=regions[state.region]||'India';if($('#mfBillingCurrency'))$('#mfBillingCurrency').textContent=`${state.currency} · ${currencySymbol(state.currency)}`;if($('#mfRegionSelect'))$('#mfRegionSelect').value=state.region;if($('#mfCurrencySelect'))$('#mfCurrencySelect').value=state.currency;if($('#mfCurrencyPreview'))$('#mfCurrencyPreview').textContent=p.monthly;if($('#mfRegionPreview'))$('#mfRegionPreview').textContent=`${regions[state.region]} · ${state.currency}`;if($('#mfMonthlyMini'))$('#mfMonthlyMini').textContent=p.monthly;if($('#mfAnnualMini'))$('#mfAnnualMini').textContent=p.annual;}
  function date(value){if(!value)return '—';const d=new Date(value);return Number.isNaN(d.getTime())?'—':d.toLocaleDateString(undefined,{day:'2-digit',month:'short',year:'numeric'});}
  function paintBilling(){
    const sub=billingInfo?.subscription,status=sub?.status||'not subscribed',active=['active','authenticated'].includes(status);
    if($('#mfSubscriptionStatus')){$('#mfSubscriptionStatus').textContent=active?'Active subscription':status.replaceAll('_',' ');$('#mfSubscriptionStatus').dataset.status=status;}
    if($('#mfCurrentPlan'))$('#mfCurrentPlan').textContent=sub?`${String(sub.plan_interval||'').replace(/^./,c=>c.toUpperCase())} · ${status.replaceAll('_',' ')}`:'No active plan';
    if($('#mfCurrentPeriod'))$('#mfCurrentPeriod').textContent=sub?.current_period_end?`Until ${date(sub.current_period_end)}`:'—';
    if($('#mfBillingNote'))$('#mfBillingNote').textContent=billingInfo?.billingConfigured?'Payments use automated Razorpay subscription checkout and server-side signature verification.':'Automated billing still needs its protected Razorpay environment configuration. INR can temporarily use the verified manual UPI fallback.';
    if($('#mfPayNow')){$('#mfPayNow').hidden=active;$('#mfPayNow').textContent=billingInfo?.billingConfigured?'Subscribe securely':'Continue to payment';}
    if($('#mfCancelPlan'))$('#mfCancelPlan').hidden=!(active&&sub?.provider==='razorpay'&&billingInfo?.role==='owner');
  }
  async function loadBilling(){
    if(!workspace?.id||!window.ModeFlowPayments?.billingStatus)return;
    try{billingInfo=await window.ModeFlowPayments.billingStatus();paintBilling();}catch(error){if($('#mfSubscriptionStatus'))$('#mfSubscriptionStatus').textContent='Unable to load status';console.warn(error);}
  }

  $$('.mf-plan-switch button').forEach(b=>b.onclick=()=>{state.interval=b.dataset.interval;localStorage.setItem('mf_plan_interval',state.interval);render();});
  $('#mfOpenRegion')?.addEventListener('click',()=>go('region'));
  $('#mfPayNow')?.addEventListener('click',async e=>{const btn=e.currentTarget;if(!window.ModeFlowPayments?.openCheckout){return notice('Payment gateway is still loading. Please try again.');}try{const result=await window.ModeFlowPayments.openCheckout(state.interval,state.currency,btn);if(!result?.cancelled)await loadBilling();}catch{}});
  $('#mfCancelPlan')?.addEventListener('click',async e=>{if(!workspace?.id||!confirm('Cancel renewal at the end of the current billing period?'))return;const b=e.currentTarget;b.disabled=true;try{const {data}=await window.tkCloud.auth.session();const r=await fetch('/api/billing/cancel',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${data?.session?.access_token||''}`},body:JSON.stringify({businessId:workspace.id})});const out=await r.json().catch(()=>({}));if(!r.ok)throw new Error(out.error||'Unable to cancel subscription');notice('Subscription renewal cancellation requested.');await loadBilling();}catch(error){notice(error.message||'Unable to cancel subscription');}finally{b.disabled=false;}});
  $('#mfRegionSelect')?.addEventListener('change',e=>{state.region=e.target.value;if(state.region==='IN')state.currency='INR';else if(state.currency==='INR')state.currency='USD';render();});
  $('#mfCurrencySelect')?.addEventListener('change',e=>{state.currency=e.target.value;render();});
  $('#mfSaveRegion')?.addEventListener('click',()=>{state.region=$('#mfRegionSelect').value;state.currency=$('#mfCurrencySelect').value;localStorage.setItem('mf_region',state.region);localStorage.setItem('modeflow-region',state.region);localStorage.setItem('mf_currency',state.currency);window.ModeFlowCurrency=state.currency;render();notice('Region and currency saved');});
  addEventListener('modeflow:workspace',e=>{workspace=e.detail;loadBilling();});addEventListener('salesdesk:billing-updated',loadBilling);
  if(window.ModeFlowBusiness){workspace=window.ModeFlowBusiness;loadBilling();}
  render();
})();