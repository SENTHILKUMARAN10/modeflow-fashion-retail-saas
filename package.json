{
  "name": "modeflow-fashion-retail-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:syntax": "node --check app.js && node --check supabase/config.js && node --check supabase/client.js && node --check supabase/modeflow-core.js && node --check supabase/realtime-app.js && node --check supabase/runtime-fixes.js"
  },
  "engines": {
    "node": ">=20"
  }
}
